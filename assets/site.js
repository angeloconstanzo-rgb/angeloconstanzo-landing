document.addEventListener('DOMContentLoaded',()=>{const n=document.querySelector('.nav');const r=document.querySelectorAll('.reveal');const c=document.querySelectorAll('.counter');const ro=new IntersectionObserver(e=>{e.forEach(x=>{if(x.isIntersecting)x.target.classList.add('visible')})},{threshold:.12});r.forEach(el=>ro.observe(el));const co=new IntersectionObserver(e=>{e.forEach(x=>{if(!x.isIntersecting)return;const el=x.target;const t=parseInt(el.dataset.target||'0',10);let cur=0;const step=Math.max(1,Math.ceil(t/42));const timer=setInterval(()=>{cur+=step;if(cur>=t){el.textContent=t+'+';clearInterval(timer)}else el.textContent=cur+'+'},30);co.unobserve(el)})},{threshold:.5});c.forEach(el=>co.observe(el));const onScroll=()=>{if(n){if(window.scrollY>20)n.classList.add('scrolled');else n.classList.remove('scrolled')}};onScroll();window.addEventListener('scroll',onScroll);document.querySelectorAll('[data-parallax]').forEach(card=>{card.addEventListener('mousemove',e=>{const rect=card.getBoundingClientRect();const x=(e.clientX-rect.left)/rect.width;const y=(e.clientY-rect.top)/rect.height;const rx=(0.5-y)*8;const ry=(x-0.5)*8;card.style.transform=`perspective(900px) rotateX(${rx}deg) rotateY(${ry}deg) translateY(-2px)`});card.addEventListener('mouseleave',()=>{card.style.transform=''})});document.querySelectorAll('[data-carousel]').forEach(carousel=>{const track=carousel.querySelector('.authority-track');if(!track)return;const step=()=>Math.min(track.clientWidth*.86,360);carousel.parentElement?.querySelector('[data-prev]')?.addEventListener('click',()=>track.scrollBy({left:-step(),behavior:'smooth'}));carousel.parentElement?.querySelector('[data-next]')?.addEventListener('click',()=>track.scrollBy({left:step(),behavior:'smooth'}));});});
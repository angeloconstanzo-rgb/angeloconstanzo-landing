document.addEventListener('DOMContentLoaded',()=>{const n=document.querySelector('.nav');const r=document.querySelectorAll('.reveal');const c=document.querySelectorAll('.counter');const ro=new IntersectionObserver(e=>{e.forEach(x=>{if(x.isIntersecting)x.target.classList.add('visible')})},{threshold:.12});r.forEach(el=>ro.observe(el));const co=new IntersectionObserver(e=>{e.forEach(x=>{if(!x.isIntersecting)return;const el=x.target;const t=parseInt(el.dataset.target||'0',10);let cur=0;const step=Math.max(1,Math.ceil(t/42));const timer=setInterval(()=>{cur+=step;if(cur>=t){el.textContent=t+'+';clearInterval(timer)}else el.textContent=cur+'+'},30);co.unobserve(el)})},{threshold:.5});c.forEach(el=>co.observe(el));window.addEventListener('scroll',()=>{if(window.scrollY>20)n.classList.add('scrolled');else n.classList.remove('scrolled')})});